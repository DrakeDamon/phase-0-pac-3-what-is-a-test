const name = "Susan";
const height = 39; // Example value, can be any number less than 40
const message = `${name} is ${height} inches tall`;

module.exports = { name, height, message };

console.log(message)